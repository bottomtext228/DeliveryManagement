import WarningCard from "../Common/WarningCard";

export default function NotFound() {

    return (
        <WarningCard
            title="Ничего не найдено"
            link="/"
            linkMessage="Главная"
        />
    )
}